<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Primary theme color of the app (sets background color of app bar) -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <!-- Background color of buttons in the app -->
        <item name="colorButtonNormal">@color/colorAccent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="button2" parent="Widget.AppCompat.Button.Colored">
        <item name="colorButtonNormal">@color/colorPrimaryDark</item>
        <item name="android:textColorPrimary">@color/textIcons</item>
        <item name="android:layout_margin">@dimen/smallMargin</item>
    </style>

    <style name="buttonReset" parent="Widget.AppCompat.Button.Colored">
        <item name="android:background">@color/colorPrimary</item>
        <item name="android:textColorPrimary">@color/textIcons</item>
        <item name="android:padding">@dimen/smallpadding</item>
        <item name="android:layout_marginTop">@dimen/smallMargin</item>
    </style>

    <style name="Team">
        <item name="android:textSize">@dimen/smallLetters</item>
        <item name="android:textColor">@color/textSecondary</item>
        <item name="android:fontFamily">sans-serif-medium</item>
        <item name="android:layout_marginTop">@dimen/smallMargin</item>
    </style>

    <style name="Score">
        <item name="android:textSize">@dimen/bigLetters</item>
        <item name="android:textColor">@color/textPrimary</item>
        <item name="android:fontFamily">sans-serif-light</item>
        <item name="android:layout_marginTop">@dimen/smallMargin</item>
        <item name="android:layout_marginBottom">@dimen/smallMargin</item>
    </style>
</resources>